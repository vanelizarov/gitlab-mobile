import 'package:flutter/Cupertino.dart';

void main() => runApp(App());

class App extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return CupertinoApp(
      title: 'Torg Gitlab UI kit demo',
      home: Container(
        child: Container(),
      ),
    );
  }
}
